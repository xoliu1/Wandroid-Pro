# 记住"跳过登录"功能

## 🎯 功能说明

解决每次启动应用都要进入登录页面的问题。现在当用户选择"跳过登录"后，下次启动应用会直接进入主页面，不再显示登录页面。

## 📝 实现方案

### 1. 添加持久化存储

在 `lib/local/KV.dart` 中添加:
- **新增 Key**: `KEY_SKIP_LOGIN` - 存储用户是否选择跳过登录
- **新增方法**:
  - `setSkipLogin(bool)` - 设置跳过登录状态
  - `hasSkippedLogin()` - 检查是否已跳过登录
  - `shouldShowLogin()` - 判断是否应显示登录页面

### 2. 修改启动逻辑

**`lib/main.dart`**:
```dart
// 修改前
home: logined ? const MainPage() : const LoginPage()

// 修改后
home: shouldShowLogin() ? const LoginPage() : const MainPage()
```

`shouldShowLogin()` 的逻辑: **未登录 且 未跳过登录** 才显示登录页面

### 3. 保存用户选择

**`lib/pages/login/login_page.dart`**:

点击"跳过登录，随便看看"按钮时:
```dart
onPressed: () {
  // 保存跳过登录的选择
  setSkipLogin(true);
  Navigator.pushReplacement(...);
}
```

### 4. 清除跳过登录标记

在以下场景会清除"跳过登录"标记，重新显示登录页面:

1. **用户成功登录时** (`login_page.dart`)
2. **用户退出登录时** (`CgiUser.dart` 的 `_clearLocalUserData()`)

## ✨ 用户体验

### 场景 1: 跳过登录
```
首次启动 → 登录页面 → 点击"跳过登录" → 进入主页
再次启动 → 直接进入主页 ✅
```

### 场景 2: 正常登录
```
首次启动 → 登录页面 → 输入账号密码登录 → 进入主页
再次启动 → 直接进入主页 ✅
```

### 场景 3: 退出登录
```
已登录状态 → 退出登录 → 清除"跳过登录"标记
再次启动 → 显示登录页面 ✅
```

### 场景 4: 已跳过登录，后续登录
```
已跳过登录 → 主动去登录 → 登录成功后清除"跳过登录"标记
再次启动 → 直接进入主页（使用登录态）✅
```

## 🔧 技术细节

### 存储方式
使用 MMKV 持久化存储，确保数据在应用重启后依然有效。

### 状态判断优先级
1. 已登录 → 直接进入主页
2. 未登录但已跳过登录 → 直接进入主页
3. 未登录且未跳过登录 → 显示登录页面

### 数据清理
- 退出登录会清除所有登录相关数据（包括跳过登录标记）
- 登录成功会清除跳过登录标记（使用正式登录态）

## 📦 涉及文件

1. `lib/local/KV.dart` - 添加存储逻辑
2. `lib/main.dart` - 修改启动判断
3. `lib/pages/login/login_page.dart` - 保存跳过登录选择
4. `lib/remote/CgiUser.dart` - 清除跳过登录标记

## 🎨 优点

- ✅ **用户友好**: 跳过登录后不再每次都显示登录页面
- ✅ **逻辑清晰**: 三种状态判断明确
- ✅ **数据安全**: 退出登录后会重新显示登录页面
- ✅ **代码简洁**: 只修改 4 个文件，新增代码少于 50 行
